import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asignacion.asignacion6',
  appName: 'Asignación 6',
  webDir: 'www'
};

export default config;
